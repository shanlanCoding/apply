<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >


<mapper namespace="">

    <!-- id:statement的id 或者叫做sql的id-->
    <!-- parameterType:声明输入参数的类型 -->
    <!-- resultType:声明输出结果的类型，应该填写pojo的全路径 -->
    <!-- #{}：输入参数的占位符，相当于jdbc的？ -->
    <!--<select id="queryUserById" parameterType="int" resultType="cn.gobyte.apply.pojo.oldUserser">-->
    <!--SELECT * FROM `oldUser` WHERE id  = #{id}-->
    <!--</select>-->

    <!--注册一个User用户到数据库内，并返回主键到systemId属性中-->
    <!--public Integer register(oldUser oldUser);-->
<!--    <insert id="register" parameterType="cn.gobyte.apply.pojo.user.oldUser" useGeneratedKeys="true" keyProperty="systemId">
      INSERT INTO `users` VALUES
      ( #{systemId}, #{id},#{password},#{tswt} ,#{mmda},#{email}, #{sid}, #{name}, #{birthd}, #{gender}, #{mz}, #{zzmm}, #{tel},
      #{address} , #{school}, #{schoolc}, #{major},#{gkbmh}, #{bkmajor}, #{mcode}, #{jl}, #{tc}, #{lxaddress}, #{yb},
      #{state}, #{sj}, #{zkzh}, #{sf} )
    </insert>-->


    <!-- 添加用户 -->
    <!--    <insert id="saveUser" parameterType="oldUser">
            insert into oldUser
            (username,birthday,sex,address) values
            (#{username},#{birthday},#{sex},#{address})
        </insert>-->


    <!--查询身份证号是否存在数据库，True存在；false不存在；-->
    <!--public Integer queryId(String id);-->
<!--    <select id="queryId" parameterType="String" resultType="Integer">
        SELECT * FROM `users` WHERE id = #{id}
    </select>-->

</mapper>
